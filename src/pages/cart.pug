doctype html
html(lang='en')
  include ../templates/head.pug
  include ../templates/blocks.pug
  
  body
    .page
      +sidebar
      +header('header--inner')
      .content.cart
        .wrapper.content__wrapper
          h1.header-common.cart__header Корзина
          +cart-table
          a.button.cart__button(href="#") оформить заказ
    +modal-call
    +modal-cart
    +footer
    include ../templates/scripts.pug